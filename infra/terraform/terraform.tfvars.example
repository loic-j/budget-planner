project_id   = "your-gcp-project-id"
region       = "asia-northeast1"
service_name = "budget-planner"
github_owner = "loic-j"
github_repo  = "budget-planner"

# Leave empty on first apply.
# After first deploy, paste the cloud_run_url output here and re-apply.
app_url = ""

# Cloudflare (set via TF_VAR env vars in CI — do not commit real values)
cloudflare_api_token = ""
cloudflare_zone_id   = ""
